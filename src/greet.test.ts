describe('greet', () => {
  /**
   * Requirement 1:
   * Write a method greet(name) that interpolates name in a simple greeting.
   * For example, when name is "Bob", the method should return a string "Hello, Bob."
   */
  test.todo('a simple greeting')
})
